#include <stdio.h>
#include <math.h>

//H(X|Y)=H(X,Y)-H(Y)
double ConditionalEntropy(double pxy[][4], double *py, int n, int m){
    double Ixy=0;
    int i=0;

    for(i=0; i < n; i++)
        Ixy += JointEntropy(pxy, n, m) - Entropy(py, n);

    return I;
}

