#include <stdio.h>
#include <math.h>

double ConditionalEntropy(double p[][4], int n, int m){
    double Ixy=0;
    int i=0;
    for(i=0; i < n; i++)
        Ixy += JointEntropy(p, n, m) - Entropy(py, n);

    return I;
}
